# Generated by rc.d/jail at 2015-10-21 15:26:10
exec.clean;							# Очищаем клетки при запуске
exec.system_user = "root";					# Главный системный пользователь
exec.jail_user = "root";					# Главный пользователь клетки
exec.start += "/bin/sh /etc/rc";				# Скрипт для запуска клетки
exec.stop = "/bin/sh /etc/rc.shutdown";				# Скрипт для остановки клетки
exec.consolelog = "/var/log/jail_${name}_console.log";		# Логи клеток
mount.devfs;							# Монтируем devfs в клетке
mount.fstab = "/etc/fstab.$name";				# Монтируем разделы в nullfs

allow.mount;							# Возможность монтировать файловые системы
allow.set_hostname = 1;						# Возможность менять hostname внутри клетки
allow.sysvipc = 1;						# Возможность использовать System V IPC
allow.chflags = 1;						# Возможность менять системные флаги файлов
allow.raw_sockets = 1;						# Возможность использования низкоуровневых сокетов (ping и тп)

